@model IEnumerable<YoureAProgrammer.Common.Models.ImagesAnswer>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Answers.Description)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ImageFullPath)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Answers.Description)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ImageFullPath)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ImageAnswerId }) |
            @Html.ActionLink("Details", "Details", new { id=item.ImageAnswerId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ImageAnswerId })
        </td>
    </tr>
}

</table>
