@model IEnumerable<YoureAProgrammer.Common.Models.ImagesQuestion>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Questions.Title)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ImageFullPath)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Questions.Title)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ImageFullPath)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ImageQuestionID }) |
            @Html.ActionLink("Details", "Details", new { id=item.ImageQuestionID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ImageQuestionID })
        </td>
    </tr>
}

</table>
